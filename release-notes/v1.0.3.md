# Claude Code Token Free v1.0.3

## Fixes

- 修复 Windows 打包版 IM（微信/钉钉）扫码绑定成功但发消息仍提示「未授权」：sidecar 统一注入 `CLAUDE_CONFIG_DIR`
- 微信扫码绑定后自动授权绑定账号；钉钉绑定成功后自动生成 IM 配对码
- 修复聊天页本地 PNG/曲线图无法渲染：支持 Windows 路径与 Markdown 图片，经 `/api/filesystem/file` 加载

## 下载说明

- macOS (Apple Silicon)：`.dmg`
- macOS (Intel)：`.dmg`
- Windows：NSIS 安装包（`.exe`）
- Linux：`.deb`
